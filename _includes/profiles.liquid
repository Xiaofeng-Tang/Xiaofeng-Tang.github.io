<div class="col">
  <a href="{% if profile.redirect %}{{ profile.redirect }}{% else %}{{ profile.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if profile.img %}
        {%
          include figure.liquid
          loading="eager"
          path=profile.img
          sizes = "250px"
          alt="profile thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">{{ profile.title }}</h2>
        <p class="card-text">{{ profile.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if profile.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ profile.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if profile.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ profile.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
